[project]
name = "socks-tls-ping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx-socks>=0.11.0",
    "httpx[http2,socks]>=0.28.1",
    "matplotlib>=3.10.8",
]
